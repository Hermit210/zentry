# Development Environment Configuration for Zentry Cloud

# Environment
ENVIRONMENT=development
DEBUG=true

# Development can use in-memory database for quick testing
USE_IN_MEMORY_DB=true
ENABLE_DEMO_MODE=true

# Supabase Configuration (Development - optional)
# SUPABASE_URL=https://your-dev-project.supabase.co
# SUPABASE_ANON_KEY=your-dev-anon-key
# SUPABASE_SERVICE_KEY=your-dev-service-key

# JWT Configuration (can be auto-generated in development)
# SECRET_KEY=auto-generated-if-not-provided
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# API Configuration
API_HOST=0.0.0.0
API_PORT=8000
API_RELOAD=true

# CORS Origins (Development)
CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000,http://localhost:3001

# Database Configuration (if using direct PostgreSQL in development)
# DATABASE_URL=postgresql://dev_user:dev_password@localhost:5432/zentry_dev